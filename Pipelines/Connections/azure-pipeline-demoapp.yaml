trigger:
- main
 
pool:
  vmImage: 'ubuntu-latest'
 
steps:
- task: KubernetesManifest@0
  inputs:
    action: 'deploy'
    kubernetesServiceConnection: 'AKS'
    namespace: 'default'
    manifests: |
      $(System.DefaultWorkingDirectory)/DemoApp/*.yaml
     # namespaceOverride: 'aks-pod'
      